<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="club.banyuan.dao.IAdminDao">
    <select id="AdminLogin" parameterType="map" resultType="Administer">
        select * from Administer where loginName=#{loginName} and password=#{password}
    </select>

</mapper>